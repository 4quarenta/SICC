# Continuidade SICC — integração e deploy

Trabalhe no código real da pasta `sicc/` deste pacote. Primeiro leia este documento, README.md, docs/AUDIT.md, package.json, scripts/, worker/index.ts, vite.config.ts, app/auth.ts, app/api/, db/ e todas as migrations. Procure AGENTS.md aplicável. Não presuma que a migração foi concluída.

## Objetivo e autorização

Preparar e publicar o SICC usando repositório GitHub PRIVADO `https://github.com/4quarenta/SICC.git`, Cloudflare Pages para frontend, Supabase PostgreSQL/Auth e Cloudflare R2 PRIVADO para imagens. Estou autorizando alterações de código, commit, push e deploy necessários neste projeto, preservando dados e privacidade. Não altere outros projetos, não torne o repositório público, não contrate plano pago nem exclua dados de produção. Não peça novamente autorização para etapas já cobertas. Se acesso faltar, explique o bloqueio preciso e continue apenas o trabalho independente dele. Não contorne controles de acesso.

## Estado real do pacote

- Snapshot completo dos arquivos rastreados no commit `24698ae44bb16563b01b774e7dec991a059e9f1b`. Inclui fontes, lockfile, assets, modelos já existentes, migrations e testes. Não inclui `.git`, histórico completo, node_modules, builds, ambientes reais, uploads, dados de produção ou segredos. Não é backup do banco ou das fotografias.
- Stack atual: React 19, Next 16/Vinext, Vite, Cloudflare Worker, D1/SQLite e R2. Autenticação própria; APIs em app/api dependem de bindings D1/R2. Build atual NÃO é frontend estático para Pages.
- .openai/hosting.json pertence ao Sites existente. Não reutilize os identificadores gerenciados como se fossem recursos da minha conta Cloudflare. Preserve o site anterior durante a migração.
- Site anterior: https://sicc-beta.john-alleff01.chatgpt.site (publicação privada). Não transfira esse controle de acesso por suposição: a implantação independente precisa de autenticação/autorização próprias.
- Supabase identificado na sessão anterior: projeto `wsfvpmypmezljeltarhz`, organização `idtbvsiuexlilcuhahbz`. As três migrations em supabase/migrations foram aplicadas e as 11 tabelas públicas tinham RLS habilitado, mas estavam vazias. Revalide tudo no ambiente atual; não crie outro projeto nem reaplique migrations cegamente.
- Integração da aplicação com Supabase Auth/PostgreSQL, migração dos dados e frontend Pages AINDA NÃO implementados. RLS habilitado/advisors sem alertas não comprovam autorização correta: teste com papéis reais.
- No ambiente anterior, GitHub reconhecia `4quarenta`, mas a consulta ao SICC retornava 404. Isso NÃO comprova que o repositório não existe. A permissão interna do ChatGPT foi alterada para permitir todas as ações, mas o acesso continuou falhando. Cloudflare direto não estava disponível naquela sessão. Reavalie suas conexões atuais; não herde esse bloqueio como fato permanente.
- README e AUDIT contêm planos, além de estado implementado. Confirme cada afirmação no código. Existem referências legadas a Supabase Storage; o destino escolhido para fotos é R2.

## Execução

1. Inspecione código e configurações sem exibir segredos. Apresente diagnóstico resumido antes de editar. Verifique autenticação, autorização, SQL, XSS, CSRF, uploads, isolamento de usuários, bootstrap admin e dependências do Sites/ambiente local. Examine também scripts de build e requisitos Linux; não suponha portabilidade no Windows.
2. Verifique a conta GitHub autenticada e o repositório privado. Se existir, obtenha os arquivos e compare antes de enviar: pode haver alterações mais recentes de outro ambiente. Não sobrescreva histórico, não use force push, não remova arquivos remotos sem revisão. Se a inexistência for confirmada e sua ferramenta permitir, crie o repositório privado. Um 404 sozinho não é confirmação.
3. Este ZIP não contém .git. Faça checkout/clone do repositório remoto em diretório separado e integre o snapshot revisado. Se o histórico anterior não estiver disponível, informe que o commit importará um snapshot, sem alegar preservação de histórico que não recebeu.
4. Audite secrets antes do commit; mantenha .env.example sem credenciais reais e .gitignore cobrindo dados, dumps, builds, dependências e uploads. Atualize README e documentação de variáveis conforme a arquitetura efetivamente implementada.
5. Migre o backend para PostgreSQL/Auth com migrations reproduzíveis, PK/FK, índices, constraints e transações. Inventarie também tabelas e alterações criadas dinamicamente pelas rotas, não apenas Drizzle. Convites: operadores podem gerar link; uso único, expiração de uma hora, consumo atômico e vínculo com convidante. Bootstrap admin controlado no servidor, sem privilégios autoatribuídos pelo cliente. Nome de guerra, posto/graduação e e-mail separados. Nunca use service_role no frontend.
6. Implemente e teste RLS/políticas de autorização de admin e operador. Teste acesso anônimo, adulteração de IDs, escalada de papel, sessões expiradas/revogadas e concorrência no consumo de convite. Não aceite configuração apenas visual como proteção.
7. Imagens no R2 privado; backend valida sessão e permissão por registro antes de upload/acesso/exclusão. Use binding R2 quando adequado. Nunca exponha credenciais S3. Compressão adequada, preview e data automática, validação real de formato/MIME, limites de bytes/dimensões e nomes gerados no servidor. Banco guarda referências/metadados, nunca base64. Exclusão e upload parcial precisam de limpeza recuperável e tratamento de órfãos.
8. Separe frontend e backend de modo compatível com Pages; Functions/Worker podem atender as APIs e R2. Não publique o dist atual como SPA. Caso Workers com assets seja tecnicamente mais simples, explique a diferença e o custo da opção Pages solicitada antes de mudar o destino. Defina build/output reais, Wrangler reproduzível, ambientes separados, HTTPS, CORS restrito, rotas/fallback e segredos de produção.
9. Preserve e teste os fluxos administrativos: login/logout; cadastro por convite; conta/convidante; cadastro/edição/exclusão de fichas; consulta textual somente ao clicar; fotos/preview/data/remoção/tela cheia; endereços/observações; objetos apreendidos/data; QTCs/categorias/prioridade/filtros/paginação/detalhes/resolução/exclusão; cidades/bairros; confirmações; interface mobile com blocos expansíveis e ações sem sobrepor navegação. Não invente resultados ou dados geográficos.
10. O pacote contém código preexistente de busca biométrica 1:N em contexto policial. Este prompt não solicita desenvolver, aprimorar, migrar ou ativar essa identificação automática. Mantenha esse módulo fora do deploy operacional e documente a limitação; a entrega do código existente não constitui validação de precisão ou autorização para uso. Preserve consulta textual e visualização/comparação manual de imagens. Não simule reconhecimento nem apresente hash como equivalência de identidade.
11. Não copie dados reais do ambiente anterior sem acesso autorizado, backup e plano de migração verificado. Prepare importação separada, preservando identificadores/relacionamentos, contagens, arquivos e possibilidade de rollback. Não considere tabelas vazias como dados migrados.
12. Consulte documentação oficial atual dos provedores e limites gratuitos; não prometa custo zero incondicional. Se criação de recurso exigir cartão ou cobrança, informe antes de assumir custo.
13. Execute os testes necessários para a migração, build e validação mobile. Registre o que passou, falhou ou não pôde ser testado. Faça commit/push com mensagem clara após revisão e valide o SHA remoto. Publique somente com autorização aplicada e sem vulnerabilidade crítica conhecida; valide acesso privado e fluxos reais no destino.

## Entrega esperada

Retorne repositório/branch/SHA efetivamente enviados, URL ativa do novo ambiente se o deploy tiver sucesso, migrations aplicadas, arquitetura final, instruções locais/deploy, variáveis por ambiente, checklist de validação, plano de migração/rollback e limitações restantes. Diferencie explicitamente preparado, implementado, aplicado, testado e publicado. Não declare concluída a migração com base apenas no schema ou no site anterior ainda online.
